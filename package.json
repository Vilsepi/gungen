{
  "name": "gungen",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite src",
    "build": "vite build src --outDir ../dist --emptyOutDir",
    "preview": "vite preview src --outDir ../dist",
    "lint": "npm run check && prettier --check \"src/**/*.{ts,js,css,html}\" \"*.{json,md}\"",
    "check": "tsc --noEmit",
    "format": "prettier --write \"src/**/*.{ts,js,css,html}\" \"*.{json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "^5.9.2",
    "vite": "^7.3.1"
  }
}
